{
  "name": "zena-bingo-cards",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sync": "npx cap sync android",
    "open": "npx cap open android",
    "build:android": "npm run build && npx cap sync android",
    "apk:debug": "cd android && .\\gradlew.bat assembleDebug",
    "apk:release": "cd android && .\\gradlew.bat assembleRelease",
    "apk:legacy": "cd android && .\\gradlew.bat assembleLegacyRelease",
    "apk:middle": "cd android && .\\gradlew.bat assembleMidRelease",
    "apk:current": "cd android && .\\gradlew.bat assembleCurrentRelease",
    "apk:all": "npm run build && npx cap sync android && cd android && .\\gradlew.bat clean assembleLegacyRelease assembleMidRelease assembleCurrentRelease",
    "apk:universal": "npm run build && npx cap sync android && cd android && .\\gradlew.bat clean assembleRelease",
    "bundle:release": "npm run build && npx cap sync android && cd android && .\\gradlew.bat clean bundleLegacyRelease",
    "bundle:all": "npm run build && npx cap sync android && cd android && .\\gradlew.bat clean bundleRelease"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@vitejs/plugin-react": "^6.0.4",
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.31",
    "tailwindcss": "^3.4.0",
    "typescript": "^7.0.2",
    "vite": "^8.1.5"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/cli": "^8.4.2",
    "@capacitor/core": "^8.4.2",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  }
}
